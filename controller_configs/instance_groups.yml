---
controller_instance_groups:
  - name: ansible_builder
    credential: AAP Admin
    is_container_group: true
    pod_spec_overide: "{{ lookup('file', 'ansible_spec/ansible-ee-builder-pod.json') | from_json }}"
